# AZChat for agents

FragGate is THE single door. True engine: aziel-runtime slug `azchat`.

Pipeline: `fraggate_list` → `fraggate_describe` slug=`azchat` → `fraggate_call`.

This Worker is the human door + counted `/download` + mesh peer.
`/v1/fraggate/*` and `/v1/mesh/*` PROXY via `AZIEL_RUNTIME`.
Mesh default OFF. GET never enables. Not SMTP. Not AZMail. Do not bridge.

Author: Aziel Eliab only.
